#pragma once

#include <cstdint>

namespace ae {

	class AeEntityManager {

		AeEntityManager();
		~AeEntityManager();

		static const int MAX_NUM_ENTITIES = 1024;

	public:

	private:

	protected:

	};
}